{
  "extends": [
    "next",
    "next/core-web-vitals",
    "prettier" // Ensure Prettier is the last configuration to avoid conflicts
  ],
  "plugins": ["prettier"],
  "rules": {
    "prettier/prettier": ["error", { "endOfLine": "auto" }]
    // Add any additional custom rules here
  }
}
